# Vercel 部署时，以下环境变量在 Vercel Dashboard 中设置：
# JWT_SECRET - 随机密钥，用于 JWT 签名
# KV_REST_API_URL / KV_REST_API_TOKEN - 由 Vercel KV 自动设置，无需手动配置

# 本地开发
JWT_SECRET=your-secret-key-here
REDIS_URL=redis://127.0.0.1:6379
